
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: ${VAULT_ADDR}
      path: kubernetes
      version: v2
      auth:
        tokenSecretRef:
          name: vault-token
          key: token
          namespace: infra
